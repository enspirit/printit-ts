import chai from 'chai';
import { expect } from 'chai';
import sinonChai from 'sinon-chai';
chai.use(sinonChai);

describe('a skeleton of unit tests', () => {
  it('works', () => {
    expect(1).to.equal(1);
  });
});
